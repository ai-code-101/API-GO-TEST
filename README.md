# my-api — Go REST API Learning Project

A simple REST API built with Go and Gin to understand how APIs work end to end.
Built as a learning project to mirror the architecture of the Peak Mobile backend.

---

## Project Structure
#if server does not run on a platform use local host onstaed of 127.0.0.1
